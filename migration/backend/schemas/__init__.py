"""
Schemas package for Badge Engine
"""
# Note: The open-badges directory uses a hyphen, which requires special handling for imports
# Use importlib or access via: import importlib.util; spec = importlib.util.spec_from_file_location(...)
# Alternatively, import from within the package using relative imports (which work fine)

__all__ = []

